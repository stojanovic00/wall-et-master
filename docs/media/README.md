# media/

Static assets for the defense deck.

- `demo.mp4` — **not included yet**. Drop the recorded end-to-end demo video here
  (same filename) as the offline/no-internet fallback for the "Demo — kompletan tok" slide.
  The slide's primary embed is a YouTube iframe (see `__VIDEO_EMBED_URL__` in `index.html`);
  this file is only the local backup shown/linked if there's no internet at defense time.
- Remaining files are figures reused from the thesis (`thesis/slike/`, `thesis/demo_ss/`).
